<!DOCTYPE html>
<html lang="en">
  <head>
    <title>PITT</title>
    <meta name="description" content="A cool thing made with Glitch">
    <link id="favicon" rel="icon" href="https://glitch.com/edit/favicon-app.ico" type="image/x-icon">
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- import the webpage's stylesheet -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  </head>
  <body class="container">
    <h1>
      University of Pittsburgh
    </h1>
    <% if (null) { %>
    <p>Database is Empty</p>
    
    <% } else { %>
    <p>There are <%= students.length %> students in the database.</p>
    <table class="table">
      <tbody><tr><th>Student ID</th><th>Name</th><th>Program Name</th><th>Course Name</th><th>Year</th></tr>
        <% students.forEach(function(student){ %>
        <tr>
          <td><%= student.studID %></td>
          <td><%= student.name %></td>
          <td><%= student.programName %></td>
          <td><%= student.courseName %></td>
          <td><%= student.year %></td>
        </tr>
        <% }); %>
      </tbody>
       </table>
    
    <p>There are <%= courses.length %> courses in the database.</p>
    <table class="table">
      <tbody><tr><th>Course ID</th><th>Course Name</th><th>Instructor Name</th></tr>
        <% courses.forEach(function(course){ %>
        <tr>
          <td><%= course.courseID %></td>
          <td><%= course.courseName %></td>
          <td><%= course.instructorName %></td>
        </tr>
        <% }); %>
      </tbody>
    </table>
    
    <p>There are <%= instructors.length %> instructors in the database.</p>
    <table class="table">
      <tbody><tr><th>Instructor ID</th><th>Instructor Name</th></tr>
        <% instructors.forEach(function(instructor){ %>
        <tr>
          <td><%= instructor.instructorID %></td>
          <td><%= instructor.instructorName %></td>
        </tr>
        <% }); %>
      </tbody>
    </table>
    
    <% } %>
    <!-- include the Glitch button to show what the webpage is about and
to make it easier for folks to view source and remix -->
    <div class="glitchButton" style="position:fixed;top:20px;right:20px;"></div>
    <script src="https://button.glitch.me/button.js"></script>
  </body>
</html>
